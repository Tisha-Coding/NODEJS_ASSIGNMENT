// importing the os module

const os = require('os');

// To print the os name
console.log("Name of the os is: ", os.type());

// To print the os release
console.log("The version of the os kernel is: ", os.release());
